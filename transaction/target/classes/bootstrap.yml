spring:
  application:
    name: customer-service
  cloud:
    kubernetes:
      config:
        name: customer-service
        namespace: epd
        enableApi: false
        paths:
          - /api/v1/namespaces/epd/configmaps/customer-service
      reload:
        enabled: true
        mode: pooling
        period: 2500
